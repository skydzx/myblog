---
import MainGridLayout from "@layouts/MainGridLayout.astro";

const routeTableBody = `
<tr><td>/</td><td>pages/[...page].astro</td><td>首页，带分页（每页 8 篇）</td></tr>
<tr><td>/posts/{slug}/</td><td>pages/posts/[...slug].astro</td><td>博客文章</td></tr>
<tr><td>/archive/</td><td>pages/archive.astro</td><td>归档页，按年份分组</td></tr>
<tr><td>/about/</td><td>pages/about.astro</td><td>关于页</td></tr>
<tr><td>/kb/</td><td>pages/kb/index.astro</td><td>知识库首页，展示分类卡片</td></tr>
<tr><td>/kb/{category}/</td><td>pages/kb/[...slug].astro</td><td>大类页：显示子分类 + 直属文章</td></tr>
<tr><td>/kb/{category}/{subcategory}/</td><td>pages/kb/[...slug].astro</td><td>子分类页：显示文章列表</td></tr>
<tr><td>/kb/{category}/{subcategory}/{article}/</td><td>pages/kb/[...slug].astro</td><td>知识库文章</td></tr>
<tr><td>/docs/</td><td>pages/docs/index.astro</td><td>项目文档首页</td></tr>
<tr><td>/docs/{page}/</td><td>pages/docs/{page}.astro</td><td>各文档页</td></tr>
<tr><td>/rss.xml</td><td>pages/rss.xml.ts</td><td>RSS 订阅源</td></tr>
<tr><td>/robots.txt</td><td>pages/robots.txt.ts</td><td>爬虫规则</td></tr>
`;
---
<MainGridLayout title="架构文档" description="项目架构、目录职责、数据模型、路由表">
	<div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative min-h-32">
		<div class="card-base z-10 px-9 py-6 relative w-full prose dark:prose-invert prose-base !max-w-none custom-md leading-relaxed">

			<a href="/docs/" class="text-sm text-[var(--text-muted)] no-underline hover:text-[var(--primary)] transition mb-4 inline-block">← 返回文档</a>

			<h1>架构文档</h1>
			<p class="text-[var(--text-muted)]">最后更新：2026-07-26。本文件为项目真源文档，所有开发规范和接口定义以此为准。</p>

			<hr class="my-8 border-[var(--border-color)]" />

			<h2 id="overview">1. 项目概览</h2>
			<p>棋密（Polybius Blog）基于 Astro 5 + Fuwari 主题构建，部署于 Cloudflare Pages。静态站点，内容由 Markdown 驱动，构建时生成 HTML。</p>

			<hr class="my-8 border-[var(--border-color)]" />

			<h2 id="directories">2. 目录职责</h2>

			<table>
				<thead><tr><th>目录</th><th>职责</th><th>约束</th></tr></thead>
				<tbody>
					<tr><td><code>src/content/posts/</code></td><td>博客文章，Markdown + frontmatter</td><td>文件名即 URL slug；每个文件一篇独立文章</td></tr>
					<tr><td><code>src/content/kb/</code></td><td>知识库，按「大类/子分类」建两级目录</td><td>一级目录 = 大类，二级目录 = 子分类；文件放最深层，文件名不含日期前缀</td></tr>
					<tr><td><code>src/content/spec/</code></td><td>固定页面（About 等）</td><td>文件名即页面标识，由对应路由页手动 <code>getEntry</code> 取用</td></tr>
					<tr><td><code>src/pages/</code></td><td>Astro 文件路由，每个 <code>.astro</code> 对应一个 URL</td><td>动态路由用 <code>[param].astro</code>，需实现 <code>getStaticPaths</code></td></tr>
					<tr><td><code>src/pages/kb/</code></td><td>知识库路由层（index + catch-all）</td><td>index.astro 为首页，[...slug].astro 统一处理任意层级</td></tr>
					<tr><td><code>src/pages/docs/</code></td><td>项目文档路由层</td><td>每页独立 <code>.astro</code> 文件，内容直写模板</td></tr>
					<tr><td><code>src/layouts/</code></td><td>页面级布局组件</td><td><code>MainGridLayout</code> 是通用壳；<code>Layout</code> 是最外层 HTML</td></tr>
					<tr><td><code>src/components/</code></td><td>可复用 UI 组件</td><td>按 control / misc / widget 分组</td></tr>
					<tr><td><code>src/plugins/</code></td><td>Remark / Rehype 插件、Expressive Code 插件</td><td>只处理 Markdown AST / 代码块渲染，不涉及 UI</td></tr>
					<tr><td><code>src/styles/</code></td><td>全局 CSS / Stylus 变量</td><td><code>variables.styl</code> 定义主题色、间距等 CSS 变量</td></tr>
					<tr><td><code>src/constants/</code></td><td>运行时常量（主题模式、分页大小、布局参数）</td><td>不依赖任何模块，可被任意文件 import</td></tr>
					<tr><td><code>src/types/</code></td><td>TypeScript 类型定义</td><td><code>config.ts</code> 内定义了所有配置项的类型</td></tr>
					<tr><td><code>src/utils/</code></td><td>工具函数（内容排序、URL 拼接、日期格式化、主题读写）</td><td>无副作用纯函数，不操作 DOM</td></tr>
					<tr><td><code>src/i18n/</code></td><td>国际化翻译表</td><td>新增语言在 <code>languages/</code> 下建文件，key 在 <code>i18nKey.ts</code> 声明</td></tr>
					<tr><td><code>src/assets/</code></td><td>静态资源（头像、Banner 图）</td><td>仅放置被 import 或 ImageWrapper 引用的图片</td></tr>
				</tbody>
			</table>

			<hr class="my-8 border-[var(--border-color)]" />

			<h2 id="collections">3. 内容集合与数据模型</h2>

			<h3>3.1 posts（博客文章）</h3>
			<p>文件路径：<code>src/content/posts/*.md</code></p>
			<table>
				<thead><tr><th>字段</th><th>类型</th><th>必填</th><th>默认值</th><th>说明</th></tr></thead>
				<tbody>
					<tr><td><code>title</code></td><td>string</td><td>是</td><td>-</td><td>文章标题，同时用作 HTML title 和 OG 标签</td></tr>
					<tr><td><code>published</code></td><td>date</td><td>是</td><td>-</td><td>发布日期，决定文章排序</td></tr>
					<tr><td><code>updated</code></td><td>date</td><td>否</td><td>-</td><td>最后修改日期</td></tr>
					<tr><td><code>draft</code></td><td>boolean</td><td>否</td><td>false</td><td>草稿不会出现在首页列表和 RSS 中</td></tr>
					<tr><td><code>description</code></td><td>string</td><td>否</td><td>""</td><td>文章摘要，用于列表卡片和 SEO</td></tr>
					<tr><td><code>image</code></td><td>string</td><td>否</td><td>""</td><td>封面图路径，相对于 posts 目录</td></tr>
					<tr><td><code>tags</code></td><td>string[]</td><td>否</td><td>[]</td><td>标签列表，用于标签筛选和侧边栏</td></tr>
					<tr><td><code>category</code></td><td>string</td><td>否</td><td>""</td><td>单一分类</td></tr>
					<tr><td><code>lang</code></td><td>string</td><td>否</td><td>""</td><td>文章语言，空则继承站点默认语言</td></tr>
				</tbody>
			</table>

			<h3>3.2 kb（知识库）</h3>
			<p>文件路径：<code>src/content/kb/</code> 下建「大类/子分类」两级目录，文章放最深层，如 <code>网络安全/渗透测试/nmap-cheatsheet.md</code></p>
			<p>两级结构：一级目录 = 大类，二级目录 = 子分类，文章文件放在最深层。当前支持两层嵌套。文件名不含日期前缀，直接写主题名。</p>
			<table>
				<thead><tr><th>字段</th><th>类型</th><th>必填</th><th>默认值</th><th>说明</th></tr></thead>
				<tbody>
					<tr><td><code>title</code></td><td>string</td><td>是</td><td>-</td><td>笔记标题</td></tr>
					<tr><td><code>date</code></td><td>date</td><td>否</td><td>-</td><td>记录日期，用于排序</td></tr>
					<tr><td><code>tags</code></td><td>string[]</td><td>否</td><td>[]</td><td>标签列表</td></tr>
					<tr><td><code>summary</code></td><td>string</td><td>否</td><td>""</td><td>一句话摘要，显示在分类列表卡片中</td></tr>
				</tbody>
			</table>

			<h3>3.3 spec（固定页面）</h3>
			<p>文件路径：<code>src/content/spec/*.md</code></p>
			<p>无 schema 约束。页面通过 <code>getEntry("spec", "about")</code> 按文件名取用。</p>

			<hr class="my-8 border-[var(--border-color)]" />

			<h2 id="routes">4. 路由表</h2>
			<table>
				<thead><tr><th>URL</th><th>文件</th><th>说明</th></tr></thead>
				<tbody set:html={routeTableBody}></tbody>
			</table>

			<hr class="my-8 border-[var(--border-color)]" />

			<h2 id="config">5. 站点配置参考</h2>
			<p>配置文件：<code>src/config.ts</code></p>

			<h3>siteConfig</h3>
			<table>
				<thead><tr><th>字段</th><th>类型</th><th>当前值</th><th>说明</th></tr></thead>
				<tbody>
					<tr><td><code>title</code></td><td>string</td><td>棋密</td><td>站点名称</td></tr>
					<tr><td><code>subtitle</code></td><td>string</td><td>Network Security · Cryptography · AI Security</td><td>副标题</td></tr>
					<tr><td><code>lang</code></td><td>string</td><td>zh_CN</td><td>默认语言代码</td></tr>
					<tr><td><code>themeColor.hue</code></td><td>number</td><td>160</td><td>主题色色相（0-360）</td></tr>
					<tr><td><code>themeColor.fixed</code></td><td>boolean</td><td>false</td><td>是否禁用访客自行调整主题色</td></tr>
					<tr><td><code>banner.enable</code></td><td>boolean</td><td>false</td><td>是否启用首页顶部 Banner</td></tr>
					<tr><td><code>toc.enable</code></td><td>boolean</td><td>true</td><td>是否启用文章目录</td></tr>
					<tr><td><code>toc.depth</code></td><td>1|2|3</td><td>2</td><td>目录最大标题层级</td></tr>
				</tbody>
			</table>

			<h3>navBarConfig</h3>
			<p>导航栏链接数组。内置预设：Home、Archive、About。自定义链接字段：name（显示文字）、url（地址）、external（是否外部链接）。</p>

			<h3>profileConfig</h3>
			<table>
				<thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead>
				<tbody>
					<tr><td><code>avatar</code></td><td>string</td><td>头像路径，相对于 src/</td></tr>
					<tr><td><code>name</code></td><td>string</td><td>昵称，显示在侧边栏</td></tr>
					<tr><td><code>bio</code></td><td>string</td><td>简介，显示在侧边栏</td></tr>
					<tr><td><code>links</code></td><td>array</td><td>社交链接，每项包含 name url icon</td></tr>
				</tbody>
			</table>

			<hr class="my-8 border-[var(--border-color)]" />

			<h2 id="plugins">6. 插件清单</h2>
			<table>
				<thead><tr><th>插件</th><th>类型</th><th>作用</th></tr></thead>
				<tbody>
					<tr><td>remark-mermaid</td><td>Remark</td><td>将 mermaid 代码块转为 HTML</td></tr>
					<tr><td>remark-math + rehype-katex</td><td>Remark + Rehype</td><td>LaTeX 数学公式渲染</td></tr>
					<tr><td>remark-reading-time</td><td>Remark</td><td>计算字数与阅读时间</td></tr>
					<tr><td>remark-excerpt</td><td>Remark</td><td>提取文章摘要</td></tr>
					<tr><td>remark-directive</td><td>Remark</td><td>解析自定义指令（note/tip/warning）</td></tr>
					<tr><td>remark-sectionize</td><td>Remark</td><td>标题间内容包裹为 section</td></tr>
					<tr><td>rehype-slug</td><td>Rehype</td><td>标题生成 id，用于锚点和 TOC</td></tr>
					<tr><td>rehype-autolink-headings</td><td>Rehype</td><td>自动添加标题锚点链接</td></tr>
					<tr><td>astro-expressive-code</td><td>Integration</td><td>代码块语法高亮（行号、折叠）</td></tr>
					<tr><td>@astrojs/svelte</td><td>Integration</td><td>Svelte 组件（搜索框、明暗切换）</td></tr>
					<tr><td>@astrojs/tailwind</td><td>Integration</td><td>Tailwind CSS 集成</td></tr>
					<tr><td>@astrojs/sitemap</td><td>Integration</td><td>自动生成 sitemap.xml</td></tr>
					<tr><td>@swup/astro</td><td>Integration</td><td>页面切换动画</td></tr>
					<tr><td>astro-icon</td><td>Integration</td><td>按需加载 SVG 图标</td></tr>
				</tbody>
			</table>

			<hr class="my-8 border-[var(--border-color)]" />

			<h2 id="new-page">7. 新增功能指南</h2>

			<h3>新增博客文章</h3>
			<pre class="card-base bg-[var(--card-bg-secondary)] p-4 font-mono text-sm overflow-x-auto mb-6 leading-relaxed text-[var(--text-muted)]">pnpm new-post my-article-name
			</pre>

			<h3>新增知识库笔记</h3>
			<pre class="card-base bg-[var(--card-bg-secondary)] p-4 font-mono text-sm overflow-x-auto mb-6 leading-relaxed text-[var(--text-muted)]">vim src/content/kb/密码学/rsa-padding-attack.md

# frontmatter 格式：
# title: RSA 填充攻击
# date: 2026-07-26
# tags: [RSA, 密码学]
# summary: PKCS#1 v1.5 填充的 Bleichenbacher 攻击原理
			</pre>

			<h3>新增文档页</h3>
			<pre class="card-base bg-[var(--card-bg-secondary)] p-4 font-mono text-sm overflow-x-auto mb-6 leading-relaxed text-[var(--text-muted)]">1. 在 src/pages/docs/ 下创建新 .astro 文件
2. 参照 architecture.astro 的结构编写
3. 在 docs/index.astro 的页面列表中添加入口
			</pre>

			<h3>新增知识库大类 / 子分类</h3>
			<pre class="card-base bg-[var(--card-bg-secondary)] p-4 font-mono text-sm overflow-x-auto mb-6 leading-relaxed text-[var(--text-muted)]">mkdir src/content/kb/新大类
mkdir src/content/kb/新大类/新子分类
# 放入 .md 文件后，路由自动生效
			</pre>

		</div>
	</div>
</MainGridLayout>
